```ts
╔══════════════════════════╗
║ SERVER STARTED SUCCESSFULLY ║
╚══════════════════════════╝
➤ Access URL: http://localhost:3000

└── /
├── auth (GET, HEAD)
│   └── / (GET, HEAD)
│       ├── google (GET, HEAD)
│       │   └── /callback (GET, HEAD)
│       ├── re
│       │   ├── gister (POST)
│       │   └── fresh (POST)
│       ├── log
│       │   ├── in (POST)
│       │   └── out (POST)
│       └── user (GET, HEAD)
└── protected/me (GET, HEAD)

```
